import React from "react";

import BlogCard from "@/components/BlogCard";

const Blog = () => {
  return (
    <section>
      <h1>Blog</h1>
      <BlogCard />
    </section>
  );
};

export default Blog;
